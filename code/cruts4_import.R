

      ###########################################
      # CAREFUL - SCRIPT RUNS APPROX 22 HOURS!!!#
      ###########################################

# first set the working directory
climate_dir <- "C:/Users/mathias/ROOT/EDU/UM/MA_MPP/MTH4911/MPP_Thesis/inputs/climate_data/cru_data"
setwd(climate_dir)

# load required packages

library(sp) # spatial package
library(raster) # spatial raster
library(rasterVis) # raster visualization
library(sf) # spatial analysis
library(R.utils) # R utilities
library(tidyverse) # Data manipulation and visualization
library(ncdf4) # read and edit ncdf files
library(lattice) # lattice for raster
library(RColorBrewer) # colors for visualization
library(cowplot) # plot_themes
library(rgdal) # reading in spatial data
library(maptools) # mapping
library(maps) # mapping, again
library(rgeos) # spatial analysis
library(magrittr) # double pipe operators
library(tictoc)

tic("with 100 rows") # start benchmarking

file_paths <- fs::dir_ls() # query the directory for list of items in it
ncin <- lapply(file_paths, raster::brick) # load NetCDF files into SpatialRaster
poly <- readRDS("C:/Users/mathias/ROOT/EDU/UM/MA_MPP/MTH4911/MPP_Thesis/inputs/shapes/gadm36_NGA_1_sf.rds") %>% 
  st_transform(proj4string(ncin$cru_ts4.05.1901.2020.cld.dat.nc))# %>% st_simplify(0.01, preserveTopology = TRUE)

extent(poly)

ncin %<>% lapply(raster::crop, poly, snap = "out") #clip raster outside the poly border
# 10 raster bricks with dimensions (lat, lon, t) (20,25,1440)

# The time frame of interest starts on "2000-01-16" and ends on "2021-12-16".

tm_nga = c(1177:1440)
ncin %<>% lapply(raster::subset, tm_nga) # crop to time frame
# 10 raster bricks with dimensions (lat, lon, t) (20,25,264)
names(ncin) <- (substring(names(ncin),22,24)) # rename list items
load ("C:/Users/mathias/ROOT/EDU/UM/MA_MPP/MTH4911/MPP_Thesis/outputs/NGA_panel.Rda") # load household data 

# plot households to see whether all parts display correctly
# plot(ncin$cld$X2020.12.16, main="HH-locations")
# plot(poly$geometry, add = TRUE)
# points(NGA_panel$LON_DD_MOD, NGA_panel$LAT_DD_MOD, pch=19, cex=0.5, col =2)
## plotting details: cex = point size, pch 0 = square

## create SPDF: SpatialPointsDataFrame()
# specify the easting (column 4) & northing (columns 3) in that order
# specify CRS proj4string: borrow CRS from chm 
# specify raster

#############################################################################
# FOR TESTING, LIMIT to 100 rows!!!!
NGA_spatial <- SpatialPointsDataFrame(
  NGA_panel[108:107], proj4string=ncin$cld$X2020.12.16@crs, NGA_panel)
# NGA_spatial <- NGA_spatial[1:10,] ###COMMENT OUT FOR THE REAL THING
#############################################################################

# next, extract climate data at a given location...
# extract circular, 20,000m (20KM) buffer

# get 10 years moving window mean variables per month

# t={1,3,5} is post-planting: September thru November YYYY.
# t={2,4,6} is post-harvest: February thru April YYYY+1

# subset rasters for monthly obs

# cld
cld01_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11
  cld01_lst = c(cld01_lst, raster::subset(ncin$cld,t ))
}
names(cld01_lst) <-c(1999:2020)

cld02_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 1
  cld02_lst = c(cld02_lst, raster::subset(ncin$cld,t ))
}
names(cld02_lst) <-c(1999:2020)

cld03_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 2
  cld03_lst = c(cld03_lst, raster::subset(ncin$cld,t ))
}
names(cld03_lst) <-c(1999:2020)

cld04_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 3
  cld04_lst = c(cld04_lst, raster::subset(ncin$cld,t ))
}
names(cld04_lst) <-c(1999:2020)

cld05_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 4
  cld05_lst = c(cld05_lst, raster::subset(ncin$cld,t ))
}
names(cld05_lst) <-c(1999:2020)

cld06_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 5
  cld06_lst = c(cld06_lst, raster::subset(ncin$cld,t ))
}
names(cld06_lst) <-c(1999:2020)

cld07_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 6
  cld07_lst = c(cld07_lst, raster::subset(ncin$cld,t ))
}
names(cld07_lst) <-c(1999:2020)

cld08_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 7
  cld08_lst = c(cld08_lst, raster::subset(ncin$cld,t ))
}
names(cld08_lst) <-c(1999:2020)

cld09_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 8
  cld09_lst = c(cld09_lst, raster::subset(ncin$cld,t ))
}
names(cld09_lst) <-c(1999:2020)

cld10_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 9
  cld10_lst = c(cld10_lst, raster::subset(ncin$cld,t ))
}
names(cld10_lst) <-c(1999:2020)

cld11_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 10
  cld11_lst = c(cld11_lst, raster::subset(ncin$cld,t ))
}
names(cld11_lst) <-c(1999:2020)

cld12_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 11
  cld12_lst = c(cld12_lst, raster::subset(ncin$cld,t ))
}
names(cld12_lst) <-c(1999:2020)

# dtr

dtr01_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11
  dtr01_lst = c(dtr01_lst, raster::subset(ncin$dtr,t ))
}
names(dtr01_lst) <-c(1999:2020)

dtr02_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 1
  dtr02_lst = c(dtr02_lst, raster::subset(ncin$dtr,t ))
}
names(dtr02_lst) <-c(1999:2020)

dtr03_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 2
  dtr03_lst = c(dtr03_lst, raster::subset(ncin$dtr,t ))
}
names(dtr03_lst) <-c(1999:2020)

dtr04_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 3
  dtr04_lst = c(dtr04_lst, raster::subset(ncin$dtr,t ))
}
names(dtr04_lst) <-c(1999:2020)

dtr05_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 4
  dtr05_lst = c(dtr05_lst, raster::subset(ncin$dtr,t ))
}
names(dtr05_lst) <-c(1999:2020)

dtr06_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 5
  dtr06_lst = c(dtr06_lst, raster::subset(ncin$dtr,t ))
}
names(dtr06_lst) <-c(1999:2020)

dtr07_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 6
  dtr07_lst = c(dtr07_lst, raster::subset(ncin$dtr,t ))
}
names(dtr07_lst) <-c(1999:2020)

dtr08_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 7
  dtr08_lst = c(dtr08_lst, raster::subset(ncin$dtr,t ))
}
names(dtr08_lst) <-c(1999:2020)

dtr09_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 8
  dtr09_lst = c(dtr09_lst, raster::subset(ncin$dtr,t ))
}
names(dtr09_lst) <-c(1999:2020)

dtr10_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 9
  dtr10_lst = c(dtr10_lst, raster::subset(ncin$dtr,t ))
}
names(dtr10_lst) <-c(1999:2020)

dtr11_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 10
  dtr11_lst = c(dtr11_lst, raster::subset(ncin$dtr,t ))
}
names(dtr11_lst) <-c(1999:2020)

dtr12_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 11
  dtr12_lst = c(dtr12_lst, raster::subset(ncin$dtr,t ))
}
names(dtr12_lst) <-c(1999:2020)

# frs

frs01_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11
  frs01_lst = c(frs01_lst, raster::subset(ncin$frs,t ))
}
names(frs01_lst) <-c(1999:2020)

frs02_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 1
  frs02_lst = c(frs02_lst, raster::subset(ncin$frs,t ))
}
names(frs02_lst) <-c(1999:2020)

frs03_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 2
  frs03_lst = c(frs03_lst, raster::subset(ncin$frs,t ))
}
names(frs03_lst) <-c(1999:2020)

frs04_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 3
  frs04_lst = c(frs04_lst, raster::subset(ncin$frs,t ))
}
names(frs04_lst) <-c(1999:2020)

frs05_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 4
  frs05_lst = c(frs05_lst, raster::subset(ncin$frs,t ))
}
names(frs05_lst) <-c(1999:2020)

frs06_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 5
  frs06_lst = c(frs06_lst, raster::subset(ncin$frs,t ))
}
names(frs06_lst) <-c(1999:2020)

frs07_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 6
  frs07_lst = c(frs07_lst, raster::subset(ncin$frs,t ))
}
names(frs07_lst) <-c(1999:2020)

frs08_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 7
  frs08_lst = c(frs08_lst, raster::subset(ncin$frs,t ))
}
names(frs08_lst) <-c(1999:2020)

frs09_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 8
  frs09_lst = c(frs09_lst, raster::subset(ncin$frs,t ))
}
names(frs09_lst) <-c(1999:2020)

frs10_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 9
  frs10_lst = c(frs10_lst, raster::subset(ncin$frs,t ))
}
names(frs10_lst) <-c(1999:2020)

frs11_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 10
  frs11_lst = c(frs11_lst, raster::subset(ncin$frs,t ))
}
names(frs11_lst) <-c(1999:2020)

frs12_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 11
  frs12_lst = c(frs12_lst, raster::subset(ncin$frs,t ))
}
names(frs12_lst) <-c(1999:2020)

# pet

pet01_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11
  pet01_lst = c(pet01_lst, raster::subset(ncin$pet,t ))
}
names(pet01_lst) <-c(1999:2020)

pet02_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 1
  pet02_lst = c(pet02_lst, raster::subset(ncin$pet,t ))
}
names(pet02_lst) <-c(1999:2020)

pet03_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 2
  pet03_lst = c(pet03_lst, raster::subset(ncin$pet,t ))
}
names(pet03_lst) <-c(1999:2020)

pet04_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 3
  pet04_lst = c(pet04_lst, raster::subset(ncin$pet,t ))
}
names(pet04_lst) <-c(1999:2020)

pet05_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 4
  pet05_lst = c(pet05_lst, raster::subset(ncin$pet,t ))
}
names(pet05_lst) <-c(1999:2020)

pet06_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 5
  pet06_lst = c(pet06_lst, raster::subset(ncin$pet,t ))
}
names(pet06_lst) <-c(1999:2020)

pet07_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 6
  pet07_lst = c(pet07_lst, raster::subset(ncin$pet,t ))
}
names(pet07_lst) <-c(1999:2020)

pet08_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 7
  pet08_lst = c(pet08_lst, raster::subset(ncin$pet,t ))
}
names(pet08_lst) <-c(1999:2020)

pet09_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 8
  pet09_lst = c(pet09_lst, raster::subset(ncin$pet,t ))
}
names(pet09_lst) <-c(1999:2020)

pet10_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 9
  pet10_lst = c(pet10_lst, raster::subset(ncin$pet,t ))
}
names(pet10_lst) <-c(1999:2020)

pet11_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 10
  pet11_lst = c(pet11_lst, raster::subset(ncin$pet,t ))
}
names(pet11_lst) <-c(1999:2020)

pet12_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 11
  pet12_lst = c(pet12_lst, raster::subset(ncin$pet,t ))
}
names(pet12_lst) <-c(1999:2020)

# pre

pre01_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11
  pre01_lst = c(pre01_lst, raster::subset(ncin$pre,t ))
}
names(pre01_lst) <-c(1999:2020)

pre02_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 1
  pre02_lst = c(pre02_lst, raster::subset(ncin$pre,t ))
}
names(pre02_lst) <-c(1999:2020)

pre03_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 2
  pre03_lst = c(pre03_lst, raster::subset(ncin$pre,t ))
}
names(pre03_lst) <-c(1999:2020)

pre04_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 3
  pre04_lst = c(pre04_lst, raster::subset(ncin$pre,t ))
}
names(pre04_lst) <-c(1999:2020)

pre05_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 4
  pre05_lst = c(pre05_lst, raster::subset(ncin$pre,t ))
}
names(pre05_lst) <-c(1999:2020)

pre06_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 5
  pre06_lst = c(pre06_lst, raster::subset(ncin$pre,t ))
}
names(pre06_lst) <-c(1999:2020)

pre07_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 6
  pre07_lst = c(pre07_lst, raster::subset(ncin$pre,t ))
}
names(pre07_lst) <-c(1999:2020)

pre08_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 7
  pre08_lst = c(pre08_lst, raster::subset(ncin$pre,t ))
}
names(pre08_lst) <-c(1999:2020)

pre09_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 8
  pre09_lst = c(pre09_lst, raster::subset(ncin$pre,t ))
}
names(pre09_lst) <-c(1999:2020)

pre10_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 9
  pre10_lst = c(pre10_lst, raster::subset(ncin$pre,t ))
}
names(pre10_lst) <-c(1999:2020)

pre11_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 10
  pre11_lst = c(pre11_lst, raster::subset(ncin$pre,t ))
}
names(pre11_lst) <-c(1999:2020)

pre12_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 11
  pre12_lst = c(pre12_lst, raster::subset(ncin$pre,t ))
}
names(pre12_lst) <-c(1999:2020)

# tmn

tmn01_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11
  tmn01_lst = c(tmn01_lst, raster::subset(ncin$tmn,t ))
}
names(tmn01_lst) <-c(1999:2020)

tmn02_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 1
  tmn02_lst = c(tmn02_lst, raster::subset(ncin$tmn,t ))
}
names(tmn02_lst) <-c(1999:2020)

tmn03_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 2
  tmn03_lst = c(tmn03_lst, raster::subset(ncin$tmn,t ))
}
names(tmn03_lst) <-c(1999:2020)

tmn04_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 3
  tmn04_lst = c(tmn04_lst, raster::subset(ncin$tmn,t ))
}
names(tmn04_lst) <-c(1999:2020)

tmn05_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 4
  tmn05_lst = c(tmn05_lst, raster::subset(ncin$tmn,t ))
}
names(tmn05_lst) <-c(1999:2020)

tmn06_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 5
  tmn06_lst = c(tmn06_lst, raster::subset(ncin$tmn,t ))
}
names(tmn06_lst) <-c(1999:2020)

tmn07_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 6
  tmn07_lst = c(tmn07_lst, raster::subset(ncin$tmn,t ))
}
names(tmn07_lst) <-c(1999:2020)

tmn08_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 7
  tmn08_lst = c(tmn08_lst, raster::subset(ncin$tmn,t ))
}
names(tmn08_lst) <-c(1999:2020)

tmn09_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 8
  tmn09_lst = c(tmn09_lst, raster::subset(ncin$tmn,t ))
}
names(tmn09_lst) <-c(1999:2020)

tmn10_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 9
  tmn10_lst = c(tmn10_lst, raster::subset(ncin$tmn,t ))
}
names(tmn10_lst) <-c(1999:2020)

tmn11_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 10
  tmn11_lst = c(tmn11_lst, raster::subset(ncin$tmn,t ))
}
names(tmn11_lst) <-c(1999:2020)

tmn12_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 11
  tmn12_lst = c(tmn12_lst, raster::subset(ncin$tmn,t ))
}
names(tmn12_lst) <-c(1999:2020)

# tmp

tmp01_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11
  tmp01_lst = c(tmp01_lst, raster::subset(ncin$tmp,t ))
}
names(tmp01_lst) <-c(1999:2020)

tmp02_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 1
  tmp02_lst = c(tmp02_lst, raster::subset(ncin$tmp,t ))
}
names(tmp02_lst) <-c(1999:2020)

tmp03_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 2
  tmp03_lst = c(tmp03_lst, raster::subset(ncin$tmp,t ))
}
names(tmp03_lst) <-c(1999:2020)

tmp04_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 3
  tmp04_lst = c(tmp04_lst, raster::subset(ncin$tmp,t ))
}
names(tmp04_lst) <-c(1999:2020)

tmp05_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 4
  tmp05_lst = c(tmp05_lst, raster::subset(ncin$tmp,t ))
}
names(tmp05_lst) <-c(1999:2020)

tmp06_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 5
  tmp06_lst = c(tmp06_lst, raster::subset(ncin$tmp,t ))
}
names(tmp06_lst) <-c(1999:2020)

tmp07_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 6
  tmp07_lst = c(tmp07_lst, raster::subset(ncin$tmp,t ))
}
names(tmp07_lst) <-c(1999:2020)

tmp08_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 7
  tmp08_lst = c(tmp08_lst, raster::subset(ncin$tmp,t ))
}
names(tmp08_lst) <-c(1999:2020)

tmp09_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 8
  tmp09_lst = c(tmp09_lst, raster::subset(ncin$tmp,t ))
}
names(tmp09_lst) <-c(1999:2020)

tmp10_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 9
  tmp10_lst = c(tmp10_lst, raster::subset(ncin$tmp,t ))
}
names(tmp10_lst) <-c(1999:2020)

tmp11_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 10
  tmp11_lst = c(tmp11_lst, raster::subset(ncin$tmp,t ))
}
names(tmp11_lst) <-c(1999:2020)

tmp12_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 11
  tmp12_lst = c(tmp12_lst, raster::subset(ncin$tmp,t ))
}
names(tmp12_lst) <-c(1999:2020)

# tmx

tmx01_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11
  tmx01_lst = c(tmx01_lst, raster::subset(ncin$tmx,t ))
}
names(tmx01_lst) <-c(1999:2020)

tmx02_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 1
  tmx02_lst = c(tmx02_lst, raster::subset(ncin$tmx,t ))
}
names(tmx02_lst) <-c(1999:2020)

tmx03_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 2
  tmx03_lst = c(tmx03_lst, raster::subset(ncin$tmx,t ))
}
names(tmx03_lst) <-c(1999:2020)

tmx04_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 3
  tmx04_lst = c(tmx04_lst, raster::subset(ncin$tmx,t ))
}
names(tmx04_lst) <-c(1999:2020)

tmx05_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 4
  tmx05_lst = c(tmx05_lst, raster::subset(ncin$tmx,t ))
}
names(tmx05_lst) <-c(1999:2020)

tmx06_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 5
  tmx06_lst = c(tmx06_lst, raster::subset(ncin$tmx,t ))
}
names(tmx06_lst) <-c(1999:2020)

tmx07_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 6
  tmx07_lst = c(tmx07_lst, raster::subset(ncin$tmx,t ))
}
names(tmx07_lst) <-c(1999:2020)

tmx08_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 7
  tmx08_lst = c(tmx08_lst, raster::subset(ncin$tmx,t ))
}
names(tmx08_lst) <-c(1999:2020)

tmx09_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 8
  tmx09_lst = c(tmx09_lst, raster::subset(ncin$tmx,t ))
}
names(tmx09_lst) <-c(1999:2020)

tmx10_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 9
  tmx10_lst = c(tmx10_lst, raster::subset(ncin$tmx,t ))
}
names(tmx10_lst) <-c(1999:2020)

tmx11_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 10
  tmx11_lst = c(tmx11_lst, raster::subset(ncin$tmx,t ))
}
names(tmx11_lst) <-c(1999:2020)

tmx12_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 11
  tmx12_lst = c(tmx12_lst, raster::subset(ncin$tmx,t ))
}
names(tmx12_lst) <-c(1999:2020)

# vap

vap01_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11
  vap01_lst = c(vap01_lst, raster::subset(ncin$vap,t ))
}
names(vap01_lst) <-c(1999:2020)

vap02_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 1
  vap02_lst = c(vap02_lst, raster::subset(ncin$vap,t ))
}
names(vap02_lst) <-c(1999:2020)

vap03_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 2
  vap03_lst = c(vap03_lst, raster::subset(ncin$vap,t ))
}
names(vap03_lst) <-c(1999:2020)

vap04_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 3
  vap04_lst = c(vap04_lst, raster::subset(ncin$vap,t ))
}
names(vap04_lst) <-c(1999:2020)

vap05_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 4
  vap05_lst = c(vap05_lst, raster::subset(ncin$vap,t ))
}
names(vap05_lst) <-c(1999:2020)

vap06_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 5
  vap06_lst = c(vap06_lst, raster::subset(ncin$vap,t ))
}
names(vap06_lst) <-c(1999:2020)

vap07_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 6
  vap07_lst = c(vap07_lst, raster::subset(ncin$vap,t ))
}
names(vap07_lst) <-c(1999:2020)

vap08_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 7
  vap08_lst = c(vap08_lst, raster::subset(ncin$vap,t ))
}
names(vap08_lst) <-c(1999:2020)

vap09_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 8
  vap09_lst = c(vap09_lst, raster::subset(ncin$vap,t ))
}
names(vap09_lst) <-c(1999:2020)

vap10_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 9
  vap10_lst = c(vap10_lst, raster::subset(ncin$vap,t ))
}
names(vap10_lst) <-c(1999:2020)

vap11_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 10
  vap11_lst = c(vap11_lst, raster::subset(ncin$vap,t ))
}
names(vap11_lst) <-c(1999:2020)

vap12_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 11
  vap12_lst = c(vap12_lst, raster::subset(ncin$vap,t ))
}
names(vap12_lst) <-c(1999:2020)

# wet

wet01_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11
  wet01_lst = c(wet01_lst, raster::subset(ncin$wet,t ))
}
names(wet01_lst) <-c(1999:2020)

wet02_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 1
  wet02_lst = c(wet02_lst, raster::subset(ncin$wet,t ))
}
names(wet02_lst) <-c(1999:2020)

wet03_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 2
  wet03_lst = c(wet03_lst, raster::subset(ncin$wet,t ))
}
names(wet03_lst) <-c(1999:2020)

wet04_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 3
  wet04_lst = c(wet04_lst, raster::subset(ncin$wet,t ))
}
names(wet04_lst) <-c(1999:2020)

wet05_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 4
  wet05_lst = c(wet05_lst, raster::subset(ncin$wet,t ))
}
names(wet05_lst) <-c(1999:2020)

wet06_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 5
  wet06_lst = c(wet06_lst, raster::subset(ncin$wet,t ))
}
names(wet06_lst) <-c(1999:2020)

wet07_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 6
  wet07_lst = c(wet07_lst, raster::subset(ncin$wet,t ))
}
names(wet07_lst) <-c(1999:2020)

wet08_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 7
  wet08_lst = c(wet08_lst, raster::subset(ncin$wet,t ))
}
names(wet08_lst) <-c(1999:2020)

wet09_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 8
  wet09_lst = c(wet09_lst, raster::subset(ncin$wet,t ))
}
names(wet09_lst) <-c(1999:2020)

wet10_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 9
  wet10_lst = c(wet10_lst, raster::subset(ncin$wet,t ))
}
names(wet10_lst) <-c(1999:2020)

wet11_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 10
  wet11_lst = c(wet11_lst, raster::subset(ncin$wet,t ))
}
names(wet11_lst) <-c(1999:2020)

wet12_lst <- list()
for (i in 1:22){
  t = i +(i-1)*11 + 11
  wet12_lst = c(wet12_lst, raster::subset(ncin$wet,t ))
}
names(wet12_lst) <-c(1999:2020)

# Now extract values with a buffer of 20 km around the EA locations

  # for all variables
  # for all months

cld01 <-cbind(raster::extract(cld01_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(cld01_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
              )
cld01 %<>% cbind(raster::extract(cld01_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2002`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2003`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2004`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2005`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2006`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2007`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2008`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2009`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2010`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2011`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2012`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2013`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2014`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2015`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2016`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2017`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2018`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2019`,
                                NGA_spatial,     # SPDF with centroids for buffer
                                buffer = 20000,     # buffer size, units depend on CRS
                                fun=mean,         # what to value to extract
                                df=TRUE))         # return a dataframe?
cld01 %<>% cbind(raster::extract(cld01_lst$`2020`,
                                  NGA_spatial,     # SPDF with centroids for buffer
                                  buffer = 20000,     # buffer size, units depend on CRS
                                  fun=mean,         # what to value to extract
                                  df=TRUE))         # return a dataframe?
cld01 <- cld01[,-seq(1,44,2)]
rm(cld01_lst)


cld02 <-cbind(raster::extract(cld02_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(cld02_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
cld02 %<>% cbind(raster::extract(cld02_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 %<>% cbind(raster::extract(cld02_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld02 <- cld02[,-seq(1,44,2)]
rm(cld02_lst)

cld03 <-cbind(raster::extract(cld03_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(cld03_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
cld03 %<>% cbind(raster::extract(cld03_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 %<>% cbind(raster::extract(cld03_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld03 <- cld03[,-seq(1,44,2)]
rm(cld03_lst)

cld04 <-cbind(raster::extract(cld04_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(cld04_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
cld04 %<>% cbind(raster::extract(cld04_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 %<>% cbind(raster::extract(cld04_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld04 <- cld04[,-seq(1,44,2)]
rm(cld04_lst)

cld05 <-cbind(raster::extract(cld05_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(cld05_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
cld05 %<>% cbind(raster::extract(cld05_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 %<>% cbind(raster::extract(cld05_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld05 <- cld05[,-seq(1,44,2)]
rm(cld05_lst)

cld06 <-cbind(raster::extract(cld06_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(cld06_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
cld06 %<>% cbind(raster::extract(cld06_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 %<>% cbind(raster::extract(cld06_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld06 <- cld06[,-seq(1,44,2)]
rm(cld06_lst)

cld07 <-cbind(raster::extract(cld07_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(cld07_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
cld07 %<>% cbind(raster::extract(cld07_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 %<>% cbind(raster::extract(cld07_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld07 <- cld07[,-seq(1,44,2)]
rm(cld07_lst)

cld08 <-cbind(raster::extract(cld08_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(cld08_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
cld08 %<>% cbind(raster::extract(cld08_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 %<>% cbind(raster::extract(cld08_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld08 <- cld08[,-seq(1,44,2)]
rm(cld08_lst)

cld09 <-cbind(raster::extract(cld09_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(cld09_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
cld09 %<>% cbind(raster::extract(cld09_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 %<>% cbind(raster::extract(cld09_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld09 <- cld09[,-seq(1,44,2)]
rm(cld09_lst)

cld10 <-cbind(raster::extract(cld10_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(cld10_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
cld10 %<>% cbind(raster::extract(cld10_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 %<>% cbind(raster::extract(cld10_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld10 <- cld10[,-seq(1,44,2)]
rm(cld10_lst)

cld11 <-cbind(raster::extract(cld11_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(cld11_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
cld11 %<>% cbind(raster::extract(cld11_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 %<>% cbind(raster::extract(cld11_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld11 <- cld11[,-seq(1,44,2)]
rm(cld11_lst)

cld12 <-cbind(raster::extract(cld12_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(cld12_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
cld12 %<>% cbind(raster::extract(cld12_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 %<>% cbind(raster::extract(cld12_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
cld12 <- cld12[,-seq(1,44,2)]
rm(cld12_lst)

# dtr

dtr01 <-cbind(raster::extract(dtr01_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(dtr01_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 %<>% cbind(raster::extract(dtr01_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr01 <- dtr01[,-seq(1,44,2)]
rm(dtr01_lst)

dtr02 <-cbind(raster::extract(dtr02_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(dtr02_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 %<>% cbind(raster::extract(dtr02_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr02 <- dtr02[,-seq(1,44,2)]
rm(dtr02_lst)

dtr03 <-cbind(raster::extract(dtr03_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(dtr03_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 %<>% cbind(raster::extract(dtr03_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr03 <- dtr03[,-seq(1,44,2)]
rm(dtr03_lst)

dtr04 <-cbind(raster::extract(dtr04_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(dtr04_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 %<>% cbind(raster::extract(dtr04_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr04 <- dtr04[,-seq(1,44,2)]
rm(dtr04_lst)

dtr05 <-cbind(raster::extract(dtr05_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(dtr05_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 %<>% cbind(raster::extract(dtr05_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr05 <- dtr05[,-seq(1,44,2)]
rm(dtr05_lst)

dtr06 <-cbind(raster::extract(dtr06_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(dtr06_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 %<>% cbind(raster::extract(dtr06_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr06 <- dtr06[,-seq(1,44,2)]
rm(dtr06_lst)

dtr07 <-cbind(raster::extract(dtr07_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(dtr07_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 %<>% cbind(raster::extract(dtr07_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr07 <- dtr07[,-seq(1,44,2)]
rm(dtr07_lst)

dtr08 <-cbind(raster::extract(dtr08_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(dtr08_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 %<>% cbind(raster::extract(dtr08_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr08 <- dtr08[,-seq(1,44,2)]
rm(dtr08_lst)

dtr09 <-cbind(raster::extract(dtr09_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(dtr09_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 %<>% cbind(raster::extract(dtr09_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr09 <- dtr09[,-seq(1,44,2)]
rm(dtr09_lst)

dtr10 <-cbind(raster::extract(dtr10_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(dtr10_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 %<>% cbind(raster::extract(dtr10_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr10 <- dtr10[,-seq(1,44,2)]
rm(dtr10_lst)

dtr11 <-cbind(raster::extract(dtr11_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(dtr11_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 %<>% cbind(raster::extract(dtr11_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr11 <- dtr11[,-seq(1,44,2)]
rm(dtr11_lst)

dtr12 <-cbind(raster::extract(dtr12_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(dtr12_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 %<>% cbind(raster::extract(dtr12_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
dtr12 <- dtr12[,-seq(1,44,2)]
rm(dtr12_lst)

# frs

frs01 <-cbind(raster::extract(frs01_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(frs01_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
frs01 %<>% cbind(raster::extract(frs01_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 %<>% cbind(raster::extract(frs01_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs01 <- frs01[,-seq(1,44,2)]
rm(frs01_lst)

frs02 <-cbind(raster::extract(frs02_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(frs02_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
frs02 %<>% cbind(raster::extract(frs02_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 %<>% cbind(raster::extract(frs02_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs02 <- frs02[,-seq(1,44,2)]
rm(frs02_lst)

frs03 <-cbind(raster::extract(frs03_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(frs03_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
frs03 %<>% cbind(raster::extract(frs03_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 %<>% cbind(raster::extract(frs03_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs03 <- frs03[,-seq(1,44,2)]
rm(frs03_lst)

frs04 <-cbind(raster::extract(frs04_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(frs04_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
frs04 %<>% cbind(raster::extract(frs04_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 %<>% cbind(raster::extract(frs04_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs04 <- frs04[,-seq(1,44,2)]
rm(frs04_lst)

frs05 <-cbind(raster::extract(frs05_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(frs05_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
frs05 %<>% cbind(raster::extract(frs05_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 %<>% cbind(raster::extract(frs05_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs05 <- frs05[,-seq(1,44,2)]
rm(frs05_lst)

frs06 <-cbind(raster::extract(frs06_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(frs06_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
frs06 %<>% cbind(raster::extract(frs06_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 %<>% cbind(raster::extract(frs06_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs06 <- frs06[,-seq(1,44,2)]
rm(frs06_lst)

frs07 <-cbind(raster::extract(frs07_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(frs07_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
frs07 %<>% cbind(raster::extract(frs07_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 %<>% cbind(raster::extract(frs07_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs07 <- frs07[,-seq(1,44,2)]
rm(frs07_lst)

frs08 <-cbind(raster::extract(frs08_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(frs08_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
frs08 %<>% cbind(raster::extract(frs08_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 %<>% cbind(raster::extract(frs08_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs08 <- frs08[,-seq(1,44,2)]
rm(frs08_lst)

frs09 <-cbind(raster::extract(frs09_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(frs09_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
frs09 %<>% cbind(raster::extract(frs09_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 %<>% cbind(raster::extract(frs09_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs09 <- frs09[,-seq(1,44,2)]
rm(frs09_lst)

frs10 <-cbind(raster::extract(frs10_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(frs10_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
frs10 %<>% cbind(raster::extract(frs10_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 %<>% cbind(raster::extract(frs10_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs10 <- frs10[,-seq(1,44,2)]
rm(frs10_lst)

frs11 <-cbind(raster::extract(frs11_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(frs11_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
frs11 %<>% cbind(raster::extract(frs11_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 %<>% cbind(raster::extract(frs11_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs11 <- frs11[,-seq(1,44,2)]
rm(frs11_lst)

frs12 <-cbind(raster::extract(frs12_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(frs12_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
frs12 %<>% cbind(raster::extract(frs12_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 %<>% cbind(raster::extract(frs12_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
frs12 <- frs12[,-seq(1,44,2)]
rm(frs12_lst)

#pet

pet01 <-cbind(raster::extract(pet01_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pet01_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pet01 %<>% cbind(raster::extract(pet01_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 %<>% cbind(raster::extract(pet01_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet01 <- pet01[,-seq(1,44,2)]
rm(pet01_lst)

pet02 <-cbind(raster::extract(pet02_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pet02_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pet02 %<>% cbind(raster::extract(pet02_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 %<>% cbind(raster::extract(pet02_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet02 <- pet02[,-seq(1,44,2)]
rm(pet02_lst)

pet03 <-cbind(raster::extract(pet03_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pet03_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pet03 %<>% cbind(raster::extract(pet03_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 %<>% cbind(raster::extract(pet03_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet03 <- pet03[,-seq(1,44,2)]
rm(pet03_lst)

pet04 <-cbind(raster::extract(pet04_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pet04_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pet04 %<>% cbind(raster::extract(pet04_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 %<>% cbind(raster::extract(pet04_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet04 <- pet04[,-seq(1,44,2)]
rm(pet04_lst)

pet05 <-cbind(raster::extract(pet05_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pet05_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pet05 %<>% cbind(raster::extract(pet05_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 %<>% cbind(raster::extract(pet05_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet05 <- pet05[,-seq(1,44,2)]
rm(pet05_lst)

pet06 <-cbind(raster::extract(pet06_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pet06_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pet06 %<>% cbind(raster::extract(pet06_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 %<>% cbind(raster::extract(pet06_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet06 <- pet06[,-seq(1,44,2)]
rm(pet06_lst)

pet07 <-cbind(raster::extract(pet07_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pet07_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pet07 %<>% cbind(raster::extract(pet07_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 %<>% cbind(raster::extract(pet07_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet07 <- pet07[,-seq(1,44,2)]
rm(pet07_lst)

pet08 <-cbind(raster::extract(pet08_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pet08_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pet08 %<>% cbind(raster::extract(pet08_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 %<>% cbind(raster::extract(pet08_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet08 <- pet08[,-seq(1,44,2)]
rm(pet08_lst)

pet09 <-cbind(raster::extract(pet09_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pet09_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pet09 %<>% cbind(raster::extract(pet09_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 %<>% cbind(raster::extract(pet09_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet09 <- pet09[,-seq(1,44,2)]
rm(pet09_lst)

pet10 <-cbind(raster::extract(pet10_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pet10_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pet10 %<>% cbind(raster::extract(pet10_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 %<>% cbind(raster::extract(pet10_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet10 <- pet10[,-seq(1,44,2)]
rm(pet10_lst)

pet11 <-cbind(raster::extract(pet11_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pet11_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pet11 %<>% cbind(raster::extract(pet11_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 %<>% cbind(raster::extract(pet11_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet11 <- pet11[,-seq(1,44,2)]
rm(pet11_lst)

pet12 <-cbind(raster::extract(pet12_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pet12_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pet12 %<>% cbind(raster::extract(pet12_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 %<>% cbind(raster::extract(pet12_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pet12 <- pet12[,-seq(1,44,2)]
rm(pet12_lst)

# pre

pre01 <-cbind(raster::extract(pre01_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pre01_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pre01 %<>% cbind(raster::extract(pre01_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 %<>% cbind(raster::extract(pre01_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre01 <- pre01[,-seq(1,44,2)]
rm(pre01_lst)

pre02 <-cbind(raster::extract(pre02_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pre02_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pre02 %<>% cbind(raster::extract(pre02_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 %<>% cbind(raster::extract(pre02_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre02 <- pre02[,-seq(1,44,2)]
rm(pre02_lst)

pre03 <-cbind(raster::extract(pre03_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pre03_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pre03 %<>% cbind(raster::extract(pre03_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 %<>% cbind(raster::extract(pre03_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre03 <- pre03[,-seq(1,44,2)]
rm(pre03_lst)

pre04 <-cbind(raster::extract(pre04_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pre04_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pre04 %<>% cbind(raster::extract(pre04_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 %<>% cbind(raster::extract(pre04_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre04 <- pre04[,-seq(1,44,2)]
rm(pre04_lst)

pre05 <-cbind(raster::extract(pre05_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pre05_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pre05 %<>% cbind(raster::extract(pre05_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 %<>% cbind(raster::extract(pre05_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre05 <- pre05[,-seq(1,44,2)]
rm(pre05_lst)

pre06 <-cbind(raster::extract(pre06_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pre06_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pre06 %<>% cbind(raster::extract(pre06_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 %<>% cbind(raster::extract(pre06_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre06 <- pre06[,-seq(1,44,2)]
rm(pre06_lst)

pre07 <-cbind(raster::extract(pre07_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pre07_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pre07 %<>% cbind(raster::extract(pre07_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 %<>% cbind(raster::extract(pre07_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre07 <- pre07[,-seq(1,44,2)]
rm(pre07_lst)

pre08 <-cbind(raster::extract(pre08_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pre08_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pre08 %<>% cbind(raster::extract(pre08_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 %<>% cbind(raster::extract(pre08_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre08 <- pre08[,-seq(1,44,2)]
rm(pre08_lst)

pre09 <-cbind(raster::extract(pre09_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pre09_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pre09 %<>% cbind(raster::extract(pre09_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 %<>% cbind(raster::extract(pre09_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre09 <- pre09[,-seq(1,44,2)]
rm(pre09_lst)

pre10 <-cbind(raster::extract(pre10_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pre10_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pre10 %<>% cbind(raster::extract(pre10_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 %<>% cbind(raster::extract(pre10_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre10 <- pre10[,-seq(1,44,2)]
rm(pre10_lst)

pre11 <-cbind(raster::extract(pre11_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pre11_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pre11 %<>% cbind(raster::extract(pre11_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 %<>% cbind(raster::extract(pre11_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre11 <- pre11[,-seq(1,44,2)]
rm(pre11_lst)

pre12 <-cbind(raster::extract(pre12_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(pre12_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
pre12 %<>% cbind(raster::extract(pre12_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 %<>% cbind(raster::extract(pre12_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
pre12 <- pre12[,-seq(1,44,2)]
rm(pre12_lst)

# tmn

tmn01 <-cbind(raster::extract(tmn01_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmn01_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 %<>% cbind(raster::extract(tmn01_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn01 <- tmn01[,-seq(1,44,2)]
rm(tmn01_lst)

tmn02 <-cbind(raster::extract(tmn02_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmn02_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 %<>% cbind(raster::extract(tmn02_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn02 <- tmn02[,-seq(1,44,2)]
rm(tmn02_lst)

tmn03 <-cbind(raster::extract(tmn03_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmn03_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 %<>% cbind(raster::extract(tmn03_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn03 <- tmn03[,-seq(1,44,2)]
rm(tmn03_lst)

tmn04 <-cbind(raster::extract(tmn04_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmn04_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 %<>% cbind(raster::extract(tmn04_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn04 <- tmn04[,-seq(1,44,2)]
rm(tmn04_lst)

tmn05 <-cbind(raster::extract(tmn05_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmn05_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 %<>% cbind(raster::extract(tmn05_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn05 <- tmn05[,-seq(1,44,2)]
rm(tmn05_lst)

tmn06 <-cbind(raster::extract(tmn06_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmn06_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 %<>% cbind(raster::extract(tmn06_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn06 <- tmn06[,-seq(1,44,2)]
rm(tmn06_lst)

tmn07 <-cbind(raster::extract(tmn07_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmn07_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 %<>% cbind(raster::extract(tmn07_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn07 <- tmn07[,-seq(1,44,2)]
rm(tmn07_lst)

tmn08 <-cbind(raster::extract(tmn08_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmn08_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 %<>% cbind(raster::extract(tmn08_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn08 <- tmn08[,-seq(1,44,2)]
rm(tmn08_lst)

tmn09 <-cbind(raster::extract(tmn09_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmn09_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 %<>% cbind(raster::extract(tmn09_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn09 <- tmn09[,-seq(1,44,2)]
rm(tmn09_lst)

tmn10 <-cbind(raster::extract(tmn10_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmn10_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 %<>% cbind(raster::extract(tmn10_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn10 <- tmn10[,-seq(1,44,2)]
rm(tmn10_lst)

tmn11 <-cbind(raster::extract(tmn11_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmn11_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 %<>% cbind(raster::extract(tmn11_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn11 <- tmn11[,-seq(1,44,2)]
rm(tmn11_lst)

tmn12 <-cbind(raster::extract(tmn12_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmn12_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 %<>% cbind(raster::extract(tmn12_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmn12 <- tmn12[,-seq(1,44,2)]
rm(tmn12_lst)

# tmp

tmp01 <-cbind(raster::extract(tmp01_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmp01_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 %<>% cbind(raster::extract(tmp01_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp01 <- tmp01[,-seq(1,44,2)]
rm(tmp01_lst)

tmp02 <-cbind(raster::extract(tmp02_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmp02_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 %<>% cbind(raster::extract(tmp02_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp02 <- tmp02[,-seq(1,44,2)]
rm(tmp02_lst)

tmp03 <-cbind(raster::extract(tmp03_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmp03_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 %<>% cbind(raster::extract(tmp03_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp03 <- tmp03[,-seq(1,44,2)]
rm(tmp03_lst)

tmp04 <-cbind(raster::extract(tmp04_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmp04_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 %<>% cbind(raster::extract(tmp04_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp04 <- tmp04[,-seq(1,44,2)]
rm(tmp04_lst)

tmp05 <-cbind(raster::extract(tmp05_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmp05_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 %<>% cbind(raster::extract(tmp05_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp05 <- tmp05[,-seq(1,44,2)]
rm(tmp05_lst)

tmp06 <-cbind(raster::extract(tmp06_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmp06_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 %<>% cbind(raster::extract(tmp06_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp06 <- tmp06[,-seq(1,44,2)]
rm(tmp06_lst)

tmp07 <-cbind(raster::extract(tmp07_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmp07_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 %<>% cbind(raster::extract(tmp07_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp07 <- tmp07[,-seq(1,44,2)]
rm(tmp07_lst)

tmp08 <-cbind(raster::extract(tmp08_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmp08_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 %<>% cbind(raster::extract(tmp08_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp08 <- tmp08[,-seq(1,44,2)]
rm(tmp08_lst)

tmp09 <-cbind(raster::extract(tmp09_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmp09_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 %<>% cbind(raster::extract(tmp09_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp09 <- tmp09[,-seq(1,44,2)]
rm(tmp09_lst)

tmp10 <-cbind(raster::extract(tmp10_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmp10_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 %<>% cbind(raster::extract(tmp10_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp10 <- tmp10[,-seq(1,44,2)]
rm(tmp10_lst)

tmp11 <-cbind(raster::extract(tmp11_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmp11_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 %<>% cbind(raster::extract(tmp11_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp11 <- tmp11[,-seq(1,44,2)]
rm(tmp11_lst)

tmp12 <-cbind(raster::extract(tmp12_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmp12_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 %<>% cbind(raster::extract(tmp12_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmp12 <- tmp12[,-seq(1,44,2)]
rm(tmp12_lst)

# tmx

tmx01 <-cbind(raster::extract(tmx01_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmx01_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 %<>% cbind(raster::extract(tmx01_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx01 <- tmx01[,-seq(1,44,2)]
rm(tmx01_lst)

tmx02 <-cbind(raster::extract(tmx02_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmx02_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 %<>% cbind(raster::extract(tmx02_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx02 <- tmx02[,-seq(1,44,2)]
rm(tmx02_lst)

tmx03 <-cbind(raster::extract(tmx03_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmx03_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 %<>% cbind(raster::extract(tmx03_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx03 <- tmx03[,-seq(1,44,2)]
rm(tmx03_lst)

tmx04 <-cbind(raster::extract(tmx04_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmx04_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 %<>% cbind(raster::extract(tmx04_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx04 <- tmx04[,-seq(1,44,2)]
rm(tmx04_lst)

tmx05 <-cbind(raster::extract(tmx05_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmx05_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 %<>% cbind(raster::extract(tmx05_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx05 <- tmx05[,-seq(1,44,2)]
rm(tmx05_lst)

tmx06 <-cbind(raster::extract(tmx06_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmx06_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 %<>% cbind(raster::extract(tmx06_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx06 <- tmx06[,-seq(1,44,2)]
rm(tmx06_lst)

tmx07 <-cbind(raster::extract(tmx07_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmx07_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 %<>% cbind(raster::extract(tmx07_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx07 <- tmx07[,-seq(1,44,2)]
rm(tmx07_lst)

tmx08 <-cbind(raster::extract(tmx08_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmx08_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 %<>% cbind(raster::extract(tmx08_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx08 <- tmx08[,-seq(1,44,2)]
rm(tmx08_lst)

tmx09 <-cbind(raster::extract(tmx09_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmx09_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 %<>% cbind(raster::extract(tmx09_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx09 <- tmx09[,-seq(1,44,2)]
rm(tmx09_lst)

tmx10 <-cbind(raster::extract(tmx10_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmx10_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 %<>% cbind(raster::extract(tmx10_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx10 <- tmx10[,-seq(1,44,2)]
rm(tmx10_lst)

tmx11 <-cbind(raster::extract(tmx11_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmx11_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 %<>% cbind(raster::extract(tmx11_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx11 <- tmx11[,-seq(1,44,2)]
rm(tmx11_lst)

tmx12 <-cbind(raster::extract(tmx12_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(tmx12_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 %<>% cbind(raster::extract(tmx12_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
tmx12 <- tmx12[,-seq(1,44,2)]
rm(tmx12_lst)

# vap

vap01 <-cbind(raster::extract(vap01_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(vap01_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
vap01 %<>% cbind(raster::extract(vap01_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 %<>% cbind(raster::extract(vap01_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap01 <- vap01[,-seq(1,44,2)]
rm(vap01_lst)

vap02 <-cbind(raster::extract(vap02_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(vap02_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
vap02 %<>% cbind(raster::extract(vap02_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 %<>% cbind(raster::extract(vap02_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap02 <- vap02[,-seq(1,44,2)]
rm(vap02_lst)

vap03 <-cbind(raster::extract(vap03_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(vap03_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
vap03 %<>% cbind(raster::extract(vap03_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 %<>% cbind(raster::extract(vap03_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap03 <- vap03[,-seq(1,44,2)]
rm(vap03_lst)

vap04 <-cbind(raster::extract(vap04_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(vap04_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
vap04 %<>% cbind(raster::extract(vap04_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 %<>% cbind(raster::extract(vap04_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap04 <- vap04[,-seq(1,44,2)]
rm(vap04_lst)

vap05 <-cbind(raster::extract(vap05_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(vap05_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
vap05 %<>% cbind(raster::extract(vap05_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 %<>% cbind(raster::extract(vap05_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap05 <- vap05[,-seq(1,44,2)]
rm(vap05_lst)

vap06 <-cbind(raster::extract(vap06_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(vap06_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
vap06 %<>% cbind(raster::extract(vap06_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 %<>% cbind(raster::extract(vap06_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap06 <- vap06[,-seq(1,44,2)]
rm(vap06_lst)

vap07 <-cbind(raster::extract(vap07_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(vap07_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
vap07 %<>% cbind(raster::extract(vap07_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 %<>% cbind(raster::extract(vap07_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap07 <- vap07[,-seq(1,44,2)]
rm(vap07_lst)

vap08 <-cbind(raster::extract(vap08_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(vap08_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
vap08 %<>% cbind(raster::extract(vap08_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 %<>% cbind(raster::extract(vap08_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap08 <- vap08[,-seq(1,44,2)]
rm(vap08_lst)

vap09 <-cbind(raster::extract(vap09_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(vap09_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
vap09 %<>% cbind(raster::extract(vap09_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 %<>% cbind(raster::extract(vap09_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap09 <- vap09[,-seq(1,44,2)]
rm(vap09_lst)

vap10 <-cbind(raster::extract(vap10_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(vap10_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
vap10 %<>% cbind(raster::extract(vap10_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 %<>% cbind(raster::extract(vap10_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap10 <- vap10[,-seq(1,44,2)]
rm(vap10_lst)

vap11 <-cbind(raster::extract(vap11_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(vap11_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
vap11 %<>% cbind(raster::extract(vap11_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 %<>% cbind(raster::extract(vap11_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap11 <- vap11[,-seq(1,44,2)]
rm(vap11_lst)

vap12 <-cbind(raster::extract(vap12_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(vap12_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
vap12 %<>% cbind(raster::extract(vap12_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 %<>% cbind(raster::extract(vap12_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
vap12 <- vap12[,-seq(1,44,2)]
rm(vap12_lst)

# wet

wet01 <-cbind(raster::extract(wet01_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(wet01_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
wet01 %<>% cbind(raster::extract(wet01_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 %<>% cbind(raster::extract(wet01_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet01 <- wet01[,-seq(1,44,2)]
rm(wet01_lst)

wet02 <-cbind(raster::extract(wet02_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(wet02_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
wet02 %<>% cbind(raster::extract(wet02_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 %<>% cbind(raster::extract(wet02_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet02 <- wet02[,-seq(1,44,2)]
rm(wet02_lst)

wet03 <-cbind(raster::extract(wet03_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(wet03_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
wet03 %<>% cbind(raster::extract(wet03_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 %<>% cbind(raster::extract(wet03_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet03 <- wet03[,-seq(1,44,2)]
rm(wet03_lst)

wet04 <-cbind(raster::extract(wet04_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(wet04_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
wet04 %<>% cbind(raster::extract(wet04_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 %<>% cbind(raster::extract(wet04_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet04 <- wet04[,-seq(1,44,2)]
rm(wet04_lst)

wet05 <-cbind(raster::extract(wet05_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(wet05_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
wet05 %<>% cbind(raster::extract(wet05_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 %<>% cbind(raster::extract(wet05_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet05 <- wet05[,-seq(1,44,2)]
rm(wet05_lst)

wet06 <-cbind(raster::extract(wet06_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(wet06_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
wet06 %<>% cbind(raster::extract(wet06_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 %<>% cbind(raster::extract(wet06_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet06 <- wet06[,-seq(1,44,2)]
rm(wet06_lst)

wet07 <-cbind(raster::extract(wet07_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(wet07_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
wet07 %<>% cbind(raster::extract(wet07_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 %<>% cbind(raster::extract(wet07_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet07 <- wet07[,-seq(1,44,2)]
rm(wet07_lst)

wet08 <-cbind(raster::extract(wet08_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(wet08_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
wet08 %<>% cbind(raster::extract(wet08_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 %<>% cbind(raster::extract(wet08_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet08 <- wet08[,-seq(1,44,2)]
rm(wet08_lst)

wet09 <-cbind(raster::extract(wet09_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(wet09_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
wet09 %<>% cbind(raster::extract(wet09_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 %<>% cbind(raster::extract(wet09_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet09 <- wet09[,-seq(1,44,2)]
rm(wet09_lst)

wet10 <-cbind(raster::extract(wet10_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(wet10_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
wet10 %<>% cbind(raster::extract(wet10_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 %<>% cbind(raster::extract(wet10_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet10 <- wet10[,-seq(1,44,2)]
rm(wet10_lst)

wet11 <-cbind(raster::extract(wet11_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(wet11_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
wet11 %<>% cbind(raster::extract(wet11_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 %<>% cbind(raster::extract(wet11_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet11 <- wet11[,-seq(1,44,2)]
rm(wet11_lst)

wet12 <-cbind(raster::extract(wet12_lst$`1999`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE),
              raster::extract(wet12_lst$`2000`,
                              NGA_spatial,     # SPDF with centroids for buffer
                              buffer = 20000,     # buffer size, units depend on CRS
                              fun=mean,         # what to value to extract
                              df=TRUE)
)
wet12 %<>% cbind(raster::extract(wet12_lst$`2001`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2002`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2003`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2004`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2005`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2006`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2007`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2008`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2009`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2010`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2011`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2012`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2013`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2014`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2015`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2016`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2017`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2018`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2019`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 %<>% cbind(raster::extract(wet12_lst$`2020`,
                                 NGA_spatial,     # SPDF with centroids for buffer
                                 buffer = 20000,     # buffer size, units depend on CRS
                                 fun=mean,         # what to value to extract
                                 df=TRUE))         # return a dataframe?
wet12 <- wet12[,-seq(1,44,2)]
rm(wet12_lst)

# clean up

rm(ncin)
rm(NGA_panel)
rm(NGA_spatial)
rm(poly)

# save climate data in Rdata format

setwd("C:/Users/mathias/ROOT/EDU/UM/MA_MPP/MTH4911/MPP_Thesis/outputs/cru")
dfs<-Filter(function(x) is.data.frame(get(x)) , ls())
for(d in dfs) {
  save(list=d, file=paste0(d, ".RData"))
}

rm(list = ls(all.names = TRUE)) #will clear all objects includes hidden objects.
gc()
toc() # end benchmarking
quit()